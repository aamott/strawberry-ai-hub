{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "description": "Execute Python code in a sandboxed environment. Use for complex logic like loops, conditionals, and multi-step operations. Access skills via 'devices.device_name.SkillName.method()'.",
  "properties": {
    "code": {
      "type": "string",
      "description": "Python code to execute. Has access to 'devices' object for skill calls across all connected devices. Use print() to show results to user."
    }
  },
  "required": ["code"],
  "additionalProperties": false
}
